package com.nukkitx.protocol.bedrock.data;

public enum HudVisibility {
   HIDE,
   RESET;

   // $FF: synthetic method
   private static HudVisibility[] $values() {
      return new HudVisibility[]{HIDE, RESET};
   }
}
