package com.nukkitx.protocol.bedrock.data.command;

public enum SoftEnumUpdateType {
   ADD,
   REMOVE,
   REPLACE;

   // $FF: synthetic method
   private static SoftEnumUpdateType[] $values() {
      return new SoftEnumUpdateType[]{ADD, REMOVE, REPLACE};
   }
}
